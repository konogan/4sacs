name: Deploy 4sacs.fr to Freebox (tar optimized)

on:
  push:
    branches:
      - main
  workflow_dispatch:

jobs:
  deploy:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Create site archive
        run: |
          echo "Creating compressed tar archive..."
          tar -czf site_static.tar.gz -C site_static .

      - name: Upload archive to Freebox /tmp
        uses: appleboy/scp-action@v0.1.7
        with:
          host: ${{ secrets.SSH_HOST }}
          username: ${{ secrets.SSH_USER }}
          key: ${{ secrets.SSH_KEY }}
          port: 2222
          source: "site_static.tar.gz"
          target: "/home/freebox/tmp"
          overwrite: true
          rm: false
          debug: true

      - name: Deploy site and restart Nginx container
        uses: appleboy/ssh-action@v1.2.0
        with:
          host: ${{ secrets.SSH_HOST }}
          username: ${{ secrets.SSH_USER }}
          key: ${{ secrets.SSH_KEY }}
          port: 2222
          script: |
            echo "[4sacs] Cleaning target directory..."
            sudo rm -rf /opt/docker/sacs/html
            sudo mkdir -p /opt/docker/sacs/html
            sudo chown -R $USER:$USER /opt/docker/sacs/html
            sudo chmod -R 755 /opt/docker/sacs/html

            echo "[4sacs] Extracting new content..."
            tar -xzf /home/freebox/tmp/site_static.tar.gz -C /opt/docker/sacs/html
            rm /home/freebox/tmp/site_static.tar.gz

            echo "[4sacs] Restarting Nginx container..."
            sudo docker compose -f /opt/docker/sacs/docker-compose.yml restart sacs

            echo "[4sacs] Deployment complete."